# ═══════════════════════════════════════════════════════════════════════════════
# ENVIRONMENT VARIABLES TEMPLATE
#
# Copia questo file in .env e modifica i valori:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════════════════════════

# === Application ===
# development | production | test
NODE_ENV=development

# Porta esposta sull'host
PORT=3000

# === CORS ===
# Origini permesse per le richieste cross-origin
# Usare '*' solo in development, specificare domini in produzione
CORS_ORIGIN=*

# === Database ===
# Path del database SQLite nel container
DB_PATH=/app/data/tasks.db
